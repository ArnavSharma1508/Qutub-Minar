{
  "dependencies": {
    "camera-controls": "^2.7.3",
    "gh-pages": "^6.1.1",
    "three": "^0.160.0"
  },
  "devDependencies": {
    "vite": "^5.0.11"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "gh-pages -d dist"
}
}
